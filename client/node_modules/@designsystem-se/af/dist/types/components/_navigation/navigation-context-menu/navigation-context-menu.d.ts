import { EventEmitter } from '../../../stencil-public-runtime';
import { DropdownItem, HTMLDir, RelativePosition } from '../navigation-dropdown/navigation-dropdown.types';
import { ContextMenuEventItem, ContextMenuItem } from './navigation-context-menu.types';
/**
 * @enums ContextMenuHorizontalPosition - navigation-context-menu-horizontal-position.enum.ts
 * @swedishName Rullgardinsmeny
 */
export declare class ContextMenu {
    private buttonRef;
    private itemRefs;
    afId: string;
    afIcon: string;
    afTitle: string;
    afMenuItems: string | ContextMenuItem[];
    afDir?: HTMLDir;
    afLang?: string;
    afAriaLabel: string;
    /**
     * När menyalternativ är valt
     * @en When menu item is activated
     */
    afMenuPosition?: RelativePosition;
    /**
     * Sätter ett förvalt värde. I React måste detta vara av typen string.
     * @en Sets a preselected value. In React this has to be of the type string.
     */
    afActiveItem: ContextMenuItem['id'];
    items: ContextMenuItem[];
    visible: boolean;
    activeItemIdx: number;
    /**
     * När menyalternativ är valt
     * @en When menu item is activated
     */
    afChangeItem: EventEmitter<ContextMenuEventItem>;
    /**
     * När fokus flyttar mellan menyalternativ
     * @en When focus change between menu items
     */
    afFocusItem: EventEmitter<ContextMenuEventItem>;
    /**
     * När menyn öppnas / stängs
     * @en When the menu opens / closes
     */
    afToggleMenu: EventEmitter<boolean>;
    /**
     * När fokus går över till ett annat element
     * @en When focus moves on to another element
     */
    afBlur: EventEmitter;
    clickHandler(e: MouseEvent): void;
    focusoutHandler(e: FocusEvent): void;
    hideMenu(): void;
    showMenu(): void;
    handleToggle(): void;
    handleChangeItem(item: ContextMenuItem, idx: number): void;
    handleFocusItem(item: ContextMenuItem, idx: number): void;
    handleRef(ref: HTMLElement, refId: string, payload: DropdownItem): void;
    syncActiveItem(): void;
    syncItems(): void;
    componentWillLoad(): void;
    get digiIcon(): string;
    render(): any;
}
