import { EventEmitter } from '../../../stencil-public-runtime';
import { Direction, OverflowMode, TabItem } from './navigation-tablist.types';
/**
 * @slot default - <digi-tablist-panel /> för varje flik
 * @swedishName Flikfält
 */
export declare class NavigationTablist {
    private tablistRef;
    private tabRefs;
    private panelsRef;
    private resizeObserver;
    private _requestAnimationFrame?;
    activeTab: number;
    selectedTab: number;
    windowTab: number;
    tabsPerWindow: number;
    tabs: TabItem[];
    listOverflows: boolean;
    showPrev: boolean;
    showNext: boolean;
    /**
     * Bestämmer flikar
     * @en Set tabs
     */
    afTabs: TabItem[] | string;
    /**
     * Bestämmer beteende då flikar överflödar
     * * @en Set overflow behaviour
     */
    afOverflowMode: OverflowMode;
    /**
     * Bestämmer aria-label på listan av flikar
     * @en Set aria-labelledby for tablist
     */
    afAriaLabelledby: string;
    /**
     * Bestämmer aria-label på listan av flikar
     * @en Set aria-label for tablist
     */
    afAriaLabel: string;
    /**
     * Flik som är aktiv vid start, använder flikens id
     * @en Active tab at start, using tab id
     */
    afInitialTab?: string;
    /**
     * Flik som är aktiv, använder flikens id
     * @en Active tab, using tab id
     */
    afActiveTab?: string;
    /**
     * Skickas när en flik aktiveras, dvs vid klick eller med space/enter‐tangentklick på markerad flik. Fliken som aktiveras finns under event.detail
     * @en Emitted when a tab is activated
     */
    afTabChange: EventEmitter<TabItem>;
    /**
     * Skickas när en flik fokusmarkeras samt vid klick. Fliken som markeras finns under event.detail
     * @en Emitted when a tab is focus‐marked
     */
    afTabFocusChange: EventEmitter<TabItem>;
    handleKey(e: KeyboardEvent): void;
    syncPanels(): void;
    handleTabPropChange(tabId: any): void;
    syncControls(): void;
    syncWindow(tabIdx: number): void;
    syncFocus(tabIdx: number): void;
    setupTabs(tabs: TabItem[] | string): void;
    setActiveTabById(id: string): void;
    setupInitialTab(): void;
    setupResizeHandler(): void;
    handleTabChange(tab: TabItem): void;
    handleResize: () => void;
    handleTabControl(direction: Direction): void;
    setActiveTabAndFocus(tabIdx: number): void;
    focusTab(tabIdx: number): void;
    moveWindow(tabIdx: number): void;
    nextTabIdx(tabIdx: number): number;
    focusCaptured(): boolean;
    componentWillLoad(): void;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    render(): any;
}
