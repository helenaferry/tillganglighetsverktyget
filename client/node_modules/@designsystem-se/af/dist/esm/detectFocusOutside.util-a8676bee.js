import { d as detectClosest } from './detectClosest.util-58a7504f.js';

function detectFocusOutside(target, selector) {
    return !detectClosest(target, selector);
}

export { detectFocusOutside as d };
